#!/bin/bash

# remove data structure from $TMPDIR
rm -r ${TMPDIR}/TRIDENT
rm -r ${TMPDIR}/histai
rm -r ${TMPDIR}/cobra
rm -r ${TMPDIR}/pp
rm -r ${TMPDIR}/hf_models_bench
rm -r ${TMPDIR}/feature_extraction
